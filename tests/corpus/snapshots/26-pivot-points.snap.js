const _series_high = context.new_var(high);
const _getHistorical_high = (offset) => _series_high.get(offset);
const _series_low = context.new_var(low);
const _getHistorical_low = (offset) => _series_low.get(offset);
indicator("Pivot Points", true);
var length = input.int(5, "Length");
var ph = Std.pivothigh(context, _series_high, length, length);
var pl = Std.pivotlow(context, _series_low, length, length);
Std.plot(ph, "Pivot High", color.red, plot.style_circles);
Std.plot(pl, "Pivot Low", color.green, plot.style_circles);