//@version=5
indicator("Supertrend", overlay=true)
factor = input.float(3.0, "Factor")
period = input.int(10, "Period")
[st, dir] = ta.supertrend(factor, period)
plot(st, "Supertrend", color=color.purple)
